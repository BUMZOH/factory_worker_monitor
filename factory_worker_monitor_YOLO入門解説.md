# YOLOによる工場作業者検知・作業時間計測 入門

## 1. はじめに

この資料では、`detect_worker.py` を題材にして、YOLOの基礎から、
USBカメラを使った人物検出、ROI（作業エリア）判定、
作業時間の計測までを順番に解説します。

今回のプログラムの目的は、単に「人を見つける」ことではありません。

> **機械の周囲に人がいる時間を計測する**

ことが最終目的です。

現在のサンプルは、そのための最小構成になっています。

処理の全体像は次のとおりです。

``` text
USBカメラ
    ↓
OpenCVで画像を1フレーム取得
    ↓
YOLOへ画像を渡す
    ↓
personだけを検出
    ↓
人物のBounding Boxを取得
    ↓
人物の中心点を計算
    ↓
中心点がROI内にあるか判定
    ↓
作業中 / 不在を判定
    ↓
作業時間を積算
    ↓
画面へ結果を表示
```

------------------------------------------------------------------------

# 2. YOLOとは

## 2.1 YOLOの役割

YOLOは画像の中から物体を検出するための物体検出モデルです。

たとえばカメラ画像の中に、

-   人
-   自動車
-   犬
-   椅子

などが写っている場合、

``` text
これは person
これは car
これは chair
```

というように、**物体の種類と位置を同時に求める**ことができます。

今回必要なのは、その中の `person` です。

------------------------------------------------------------------------

## 2.2 画像分類との違い

画像分類では、画像全体について、

``` text
この画像には人が写っている
```

という判定を行います。

一方、物体検出では、

``` text
人が写っている
さらに、その人は画像のこの位置にいる
```

というところまで求めます。

今回のように、

> 人が機械の作業エリア内にいるか？

を判定するためには、人物の**位置**が必要です。

そのため画像分類ではなく、物体検出を使用します。

------------------------------------------------------------------------

## 2.3 Bounding Box

YOLOが物体を検出すると、その物体を囲む長方形が得られます。

これを **Bounding Box（バウンディングボックス）** と呼びます。

``` text
(x1, y1)
   ●─────────────┐
   │             │
   │    person   │
   │             │
   └─────────────●
              (x2, y2)
```

今回のコードでは、

``` python
x1, y1, x2, y2 = box.xyxy[0].tolist()
```

で取得しています。

それぞれ、

  変数   意味
  ------ -------------
  `x1`   左端のX座標
  `y1`   上端のY座標
  `x2`   右端のX座標
  `y2`   下端のY座標

です。

------------------------------------------------------------------------

# 3. YOLOv8nについて

今回使用しているモデルは、

``` python
MODEL_PATH = "yolov8n.pt"
```

です。

`YOLOv8` はYOLO系列の物体検出モデルの一つです。

末尾の `n` は **nano** を意味します。

nanoモデルは比較的小さく軽量なので、

-   学習用
-   USBカメラによるリアルタイム処理
-   CPUでの動作確認
-   最初のプロトタイプ

などに向いています。

高精度な大きいモデルをいきなり使うより、まず軽いモデルで
システム全体を完成させる方が今回の用途には適しています。

------------------------------------------------------------------------

# 4. 学習済みモデルとは

今回のプログラムでは、自分でYOLOを学習させていません。

``` python
model = YOLO(MODEL_PATH)
```

として、すでに学習されたモデルを読み込んでいます。

`yolov8n.pt` には、一般的な物体を認識するための学習結果が
保存されています。

そのため、

``` python
model.predict(...)
```

するだけで人物を検出できます。

今回の目的は「人」を検出することなので、まずは既存の
学習済みモデルで十分です。

一方、

``` text
ヘルメット
特定の部品
特定の製品
機械固有の状態
```

など、標準モデルが知らない対象を検出したい場合は、
独自データを準備して追加学習することを検討します。

------------------------------------------------------------------------

# 5. confidenceとは

YOLOは検出結果について、

> どの程度その物体だと確信しているか

を表す信頼度を持っています。

今回のコードでは、

``` python
CONFIDENCE = 0.5
```

として、

``` python
conf=CONFIDENCE
```

を指定しています。

概念的には、

``` text
personらしさ 0.92 → 採用
personらしさ 0.76 → 採用
personらしさ 0.53 → 採用
personらしさ 0.31 → 不採用
```

という使い方になります。

しきい値を高くすると誤検出は減りやすくなりますが、
本物の人物を見逃す可能性も高くなります。

逆に低くすると人物を拾いやすくなりますが、誤検出も
増える可能性があります。

工場で実際に使う場合は、カメラ位置や照明条件を確認しながら
調整する値です。

------------------------------------------------------------------------

# 6. COCOクラスとperson

今回の重要な部分です。

``` python
result = model.predict(
    source=frame,
    classes=[0],
    conf=CONFIDENCE,
    verbose=False,
)[0]
```

`classes=[0]` によって、検出対象をクラス番号0に限定しています。

今回使用している標準モデルでは、クラス0が `person` です。

つまり、

``` python
classes=[0]
```

は、

> **人だけを検出してください**

という指定です。

工場内には機械、椅子、台車など様々な物体が写る可能性が
ありますが、今回必要なのは人物だけなので、この指定によって
処理目的を明確にしています。

------------------------------------------------------------------------

# 7. OpenCVの役割

YOLOが「物体検出」を担当するのに対し、OpenCVは主に、

-   USBカメラから画像を取得
-   四角形を描画
-   点を描画
-   文字を描画
-   ウィンドウを表示
-   キー入力を取得

といった画像処理・表示部分を担当しています。

役割を大きく分けると、

``` text
OpenCV
    ↓
カメラ・画像・表示を担当

YOLO
    ↓
画像の内容を認識するAIを担当
```

と考えると分かりやすいです。

------------------------------------------------------------------------

# 8. モジュール

今回使用しているモジュールは次の3つです。

``` python
import time

import cv2
from ultralytics import YOLO
```

## time

Python標準ライブラリです。

今回、

``` python
time.monotonic()
```

によって経過時間を計測します。

追加インストールは不要です。

## cv2

OpenCVです。

インストールは、

``` powershell
pip install opencv-python
```

です。

## ultralytics

YOLOをPythonから扱うために使用します。

``` powershell
pip install ultralytics
```

でインストールできます。

まとめてインストールする場合は、

``` powershell
pip install ultralytics opencv-python
```

です。

------------------------------------------------------------------------

# 9. プログラム全文

今回の出発地点となるコードを全文掲載します。

``` python
import time

import cv2
from ultralytics import YOLO


MODEL_PATH = "yolov8n.pt"
CAMERA_NO = 0

CONFIDENCE = 0.5

# 作業エリア
ROI_X1 = 150
ROI_Y1 = 150
ROI_X2 = 550
ROI_Y2 = 450


model = YOLO(MODEL_PATH)

camera = cv2.VideoCapture(CAMERA_NO)

working = False
work_start_time = None
total_work_time = 0.0


while True:
    success, frame = camera.read()

    if not success:
        break

    result = model.predict(
        source=frame,
        classes=[0],
        conf=CONFIDENCE,
        verbose=False,
    )[0]

    person_in_area = False

    for box in result.boxes:
        x1, y1, x2, y2 = box.xyxy[0].tolist()

        center_x = int((x1 + x2) / 2)
        center_y = int((y1 + y2) / 2)

        if (
            ROI_X1 <= center_x <= ROI_X2
            and ROI_Y1 <= center_y <= ROI_Y2
        ):
            person_in_area = True

        cv2.rectangle(
            frame,
            (int(x1), int(y1)),
            (int(x2), int(y2)),
            (0, 255, 0),
            2,
        )

        cv2.circle(
            frame,
            (center_x, center_y),
            5,
            (0, 0, 255),
            -1,
        )

    now = time.monotonic()

    if person_in_area:
        if not working:
            working = True
            work_start_time = now

    else:
        if working:
            total_work_time += now - work_start_time
            working = False
            work_start_time = None

    current_work_time = total_work_time

    if working:
        current_work_time += now - work_start_time

    cv2.rectangle(
        frame,
        (ROI_X1, ROI_Y1),
        (ROI_X2, ROI_Y2),
        (255, 0, 0),
        3,
    )

    status = "WORKING" if person_in_area else "NO PERSON"

    cv2.putText(
        frame,
        status,
        (20, 40),
        cv2.FONT_HERSHEY_SIMPLEX,
        1.0,
        (0, 0, 255),
        2,
    )

    cv2.putText(
        frame,
        f"Work Time: {current_work_time:.1f} sec",
        (20, 80),
        cv2.FONT_HERSHEY_SIMPLEX,
        0.8,
        (0, 0, 255),
        2,
    )

    cv2.imshow("Person Detection", frame)

    key = cv2.waitKey(1)

    if key == 27:
        break


camera.release()
cv2.destroyAllWindows()
```

------------------------------------------------------------------------

# 10. 定数設定

``` python
MODEL_PATH = "yolov8n.pt"
CAMERA_NO = 0

CONFIDENCE = 0.5
```

`MODEL_PATH` は使用するYOLOモデルです。

`CAMERA_NO = 0` はOpenCVで使用するカメラ番号です。

通常、最初のカメラが `0` です。

複数カメラがある場合、

``` python
CAMERA_NO = 1
```

などになることがあります。

------------------------------------------------------------------------

# 11. ROI（Region of Interest）

今回のプログラムで非常に重要なのがROIです。

ROIは **Region of Interest** の略で、日本語では
「関心領域」などと呼ばれます。

今回は、

> **人が作業しているとみなす範囲**

として使用します。

``` python
ROI_X1 = 150
ROI_Y1 = 150
ROI_X2 = 550
ROI_Y2 = 450
```

イメージは、

``` text
(150, 150)
    ●─────────────────┐
    │                 │
    │   作業エリア    │
    │                 │
    └─────────────────●
                   (550, 450)
```

です。

カメラ映像全体ではなく、機械の前など必要な場所だけを
判定対象にできます。

------------------------------------------------------------------------

# 12. YOLOモデルの読み込み

``` python
model = YOLO(MODEL_PATH)
```

ここでYOLOモデルを読み込んでいます。

`model` は、以降、

``` python
model.predict(...)
```

として推論に使用します。

重要なのは、ループの中で毎回、

``` python
model = YOLO(...)
```

としていないことです。

モデルは最初に1回読み込み、何度も再利用します。

------------------------------------------------------------------------

# 13. USBカメラを開く

``` python
camera = cv2.VideoCapture(CAMERA_NO)
```

OpenCVにUSBカメラを開かせています。

この `camera` から、

``` python
success, frame = camera.read()
```

として画像を取得できます。

------------------------------------------------------------------------

# 14. 動画は静止画の連続

ここは非常に大切な考え方です。

コンピュータから見ると、カメラ映像は、

``` text
画像1
画像2
画像3
画像4
画像5
...
```

という静止画の連続として処理できます。

そのため、

``` python
while True:
    success, frame = camera.read()
```

で画像を1枚ずつ取得し、そのたびにYOLOへ渡しています。

つまり、

``` text
1フレーム取得
↓
YOLO検出
↓
表示
↓
次のフレーム取得
↓
YOLO検出
↓
表示
```

を高速に繰り返しているため、動画として見えます。

------------------------------------------------------------------------

# 15. camera.read()

``` python
success, frame = camera.read()
```

戻り値は2つあります。

`success` は画像取得に成功したかどうかです。

`frame` は取得した画像データです。

失敗した場合、

``` python
if not success:
    break
```

でループを終了します。

------------------------------------------------------------------------

# 16. YOLOによる推論

``` python
result = model.predict(
    source=frame,
    classes=[0],
    conf=CONFIDENCE,
    verbose=False,
)[0]
```

ここがAIによる人物検出の中心です。

## source=frame

YOLOへ解析する画像を渡します。

今回はUSBカメラから取得した1フレームです。

## classes=\[0\]

人物だけを検出対象にします。

## conf=CONFIDENCE

信頼度のしきい値を指定します。

## verbose=False

コンソールへの詳細な推論ログを抑制します。

## \[0\]

`predict()` は複数画像を扱える形で結果を返すため、 今回の1枚分の結果を
`[0]` で取り出しています。

------------------------------------------------------------------------

# 17. 検出された人物を順番に処理する

``` python
for box in result.boxes:
```

人物が3人検出された場合、概念的には、

``` text
box 1 → 人物A
box 2 → 人物B
box 3 → 人物C
```

という形で順番に処理します。

今回必要なのは、

> 誰か1人でも作業エリアにいるか

です。

そのため人物の個人識別はしていません。

------------------------------------------------------------------------

# 18. Bounding Boxの座標取得

``` python
x1, y1, x2, y2 = box.xyxy[0].tolist()
```

YOLOの検出結果から人物を囲む長方形の座標を取得します。

``` text
        x方向 →

      x1          x2
       ↓           ↓
y1 →   ●───────────┐
       │           │
       │  person   │
       │           │
y2 →   └───────────●
```

画像座標では通常、

``` text
左上 = (0, 0)
```

です。

右へ進むほどXが増え、下へ進むほどYが増えます。

------------------------------------------------------------------------

# 19. 人物の中心点

``` python
center_x = int((x1 + x2) / 2)
center_y = int((y1 + y2) / 2)
```

Bounding Boxの中央座標を求めています。

X座標は、

$$
x_{center} = \frac{x_1 + x_2}{2}
$$

Y座標は、

$$
y_{center} = \frac{y_1 + y_2}{2}
$$

です。

この中心点を、

> その人物が現在いる位置

の代表値として使用しています。

------------------------------------------------------------------------

# 20. ROI内判定

``` python
if (
    ROI_X1 <= center_x <= ROI_X2
    and ROI_Y1 <= center_y <= ROI_Y2
):
    person_in_area = True
```

中心点のX座標とY座標の両方がROI内なら、

``` python
person_in_area = True
```

にします。

つまり、

``` text
人物検出
    ↓
中心点を計算
    ↓
中心点がROI内
    ↓
person_in_area = True
    ↓
作業中
```

という判定です。

------------------------------------------------------------------------

# 21. person_in_areaを毎フレームFalseに戻す理由

ループ内の最初で、

``` python
person_in_area = False
```

としています。

これは非常に重要です。

前回のフレームで人がいたとしても、

> 今回のフレームにも人がいるとは限らない

からです。

したがって毎フレーム、

``` text
まず「いない」と仮定
↓
YOLOの結果を確認
↓
ROI内に人物がいたらTrue
```

としています。

------------------------------------------------------------------------

# 22. 検出枠の描画

``` python
cv2.rectangle(
    frame,
    (int(x1), int(y1)),
    (int(x2), int(y2)),
    (0, 255, 0),
    2,
)
```

人物のBounding Boxを画面へ描画します。

OpenCVでは色を基本的に、

``` text
(B, G, R)
```

の順番で指定します。

``` python
(0, 255, 0)
```

は緑です。

最後の `2` は線の太さです。

------------------------------------------------------------------------

# 23. 中心点の描画

``` python
cv2.circle(
    frame,
    (center_x, center_y),
    5,
    (0, 0, 255),
    -1,
)
```

人物の中心点を丸で表示します。

`5` は円の半径です。

``` python
(0, 0, 255)
```

は赤です。

`-1` は円の内部を塗りつぶす指定です。

この点を見ることで、

> なぜこの人物がROI内と判定されたのか

を視覚的に確認できます。

デバッグ時には非常に役立ちます。

------------------------------------------------------------------------

# 24. time.monotonic()

``` python
now = time.monotonic()
```

作業時間計測には `time.monotonic()` を使用しています。

これは経過時間を測る用途に適した時計です。

通常の日時とは違い、

``` text
2026-09-10 10:30:00
```

のような現在日時を取得する目的ではありません。

重要なのは、

``` python
終了時刻 - 開始時刻
```

として**経過秒数を安全に計算する**ことです。

将来SQLiteへ、

``` text
作業開始日時
作業終了日時
```

を保存する場合は、別途 `datetime` などを使う設計が考えられます。

------------------------------------------------------------------------

# 25. 作業開始判定

``` python
if person_in_area:
    if not working:
        working = True
        work_start_time = now
```

ROI内に人がいて、まだ `working` がFalseなら、
その瞬間を作業開始とします。

``` text
working = False
person_in_area = True
        ↓
作業開始
        ↓
working = True
work_start_time = now
```

です。

------------------------------------------------------------------------

# 26. なぜworkingが必要なのか

もし、

``` python
if person_in_area:
    work_start_time = now
```

だけにすると、人がいる間は毎フレーム開始時刻が更新されてしまいます。

そこで、

``` python
if not working:
```

によって、

> 作業していない状態から作業中へ変化した瞬間だけ

開始時刻を保存します。

これは状態管理の基本的な考え方です。

------------------------------------------------------------------------

# 27. 作業終了判定

``` python
else:
    if working:
        total_work_time += now - work_start_time
        working = False
        work_start_time = None
```

人がROIからいなくなり、それまで作業中だった場合、

``` python
now - work_start_time
```

で今回の作業時間を求めます。

そして、

``` python
total_work_time += ...
```

で累積します。

------------------------------------------------------------------------

# 28. += の意味

例えば、

``` text
これまでの作業時間 = 100秒
今回の作業時間     = 30秒
```

なら、

``` python
total_work_time += 30
```

によって、

``` text
total_work_time = 130秒
```

となります。

複数回の作業を積算できます。

------------------------------------------------------------------------

# 29. 現在進行中の時間も表示する

``` python
current_work_time = total_work_time

if working:
    current_work_time += now - work_start_time
```

`total_work_time` に入っているのは、基本的に終了済み区間の
累積時間です。

しかし現在も作業中なら、

``` text
作業開始 → 現在
```

までの時間も画面へ表示したいので、その分を一時的に `current_work_time`
へ加えています。

この処理によって、画面上の時間が作業中に増え続けます。

------------------------------------------------------------------------

# 30. ROIを画面へ表示する

``` python
cv2.rectangle(
    frame,
    (ROI_X1, ROI_Y1),
    (ROI_X2, ROI_Y2),
    (255, 0, 0),
    3,
)
```

作業エリアそのものを長方形として表示しています。

OpenCVのBGR表記なので、

``` python
(255, 0, 0)
```

は青です。

これによって、

> どの範囲を作業エリアとして判定しているか

を確認できます。

------------------------------------------------------------------------

# 31. ステータス表示

``` python
status = "WORKING" if person_in_area else "NO PERSON"
```

これは条件式（三項演算子的な書き方）です。

通常の `if` で書けば、

``` python
if person_in_area:
    status = "WORKING"
else:
    status = "NO PERSON"
```

と同じ意味です。

------------------------------------------------------------------------

# 32. cv2.putText()

``` python
cv2.putText(...)
```

は画像上に文字を描画するOpenCVの関数です。

今回、

``` text
WORKING
NO PERSON
Work Time: xxx sec
```

を表示するために使っています。

AIの判定結果を人間がその場で確認できるため、
実験段階では重要な表示です。

------------------------------------------------------------------------

# 33. cv2.imshow()

``` python
cv2.imshow("Person Detection", frame)
```

処理後の画像をウィンドウへ表示します。

表示している `frame` には、

-   USBカメラ画像
-   人物Bounding Box
-   人物中心点
-   ROI
-   WORKING / NO PERSON
-   累積作業時間

がすべて描画されています。

------------------------------------------------------------------------

# 34. ESCキーで終了

``` python
key = cv2.waitKey(1)

if key == 27:
    break
```

`cv2.waitKey(1)` でキー入力を確認しています。

ESCキーのキーコードが `27` なので、

``` python
if key == 27:
```

でESCキーを検出してループを終了します。

------------------------------------------------------------------------

# 35. カメラとウィンドウの終了処理

``` python
camera.release()
cv2.destroyAllWindows()
```

`camera.release()` はカメラを解放します。

`cv2.destroyAllWindows()` はOpenCVが作成したウィンドウを 閉じます。

プログラム終了時に資源をきちんと解放するための処理です。

------------------------------------------------------------------------

# 36. このプログラムでまだ行っていないこと

現在のサンプルは出発地点として非常にシンプルです。

まだ、

-   人物追跡
-   個人識別
-   複数ROI
-   SQLite保存
-   検出切れ対策
-   日別集計
-   機械の稼働信号との連携

などは行っていません。

特に実際の工場では、一瞬人物を見失っただけで作業終了に
しない工夫が重要になります。

------------------------------------------------------------------------

# 37. 今後の改善1：中心点から足元判定へ

現在は、

``` python
center_x = int((x1 + x2) / 2)
center_y = int((y1 + y2) / 2)
```

で人物の中心点を使っています。

工場で、

> 人がどこに立っているか

を判断するなら、Bounding Boxの下中央、つまり足元を
代表点として使う方法も有力です。

``` python
person_x = int((x1 + x2) / 2)
person_y = int(y2)
```

イメージは、

``` text
┌─────────┐
│   頭    │
│         │
│   人    │
│         │
└────●────┘
     ↑
   足元
```

です。

この点がROI内かどうかを判定すれば、

> 作業者がどこに立っているか

という意味に近い判定になります。

------------------------------------------------------------------------

# 38. 今後の改善2：検出切れを吸収する

実際のカメラでは、

``` text
person
person
person
検出なし
person
person
```

のような一瞬の検出切れが発生する可能性があります。

現在のコードでは、その瞬間に作業終了になります。

そこで将来は、

``` text
人物が消えた
    ↓
すぐに作業終了しない
    ↓
例えば3秒待つ
    ↓
3秒間検出されなければ作業終了
```

というオフディレイ処理を入れると安定します。

PLCのタイマーのような感覚で考えると分かりやすいでしょう。

------------------------------------------------------------------------

# 39. 今後の改善3：SQLiteへ保存

最終的には、作業区間ごとに、

``` text
machine_no
start_time
end_time
duration_sec
```

などをSQLiteへ保存できます。

例えば、

``` text
MC01 | 08:12:20 | 08:18:35 | 375
MC01 | 08:22:10 | 08:25:40 | 210
```

というデータを残せば、

-   1日の有人作業時間
-   時間帯別の有人時間
-   機械別の有人率
-   無人運転時間

などを分析できます。

------------------------------------------------------------------------

# 40. YOLOと今回のプログラムの役割分担

ここは整理して覚えておくと理解しやすいです。

``` text
YOLO
│
├─ 画像からpersonを探す
├─ Bounding Boxを返す
└─ 信頼度を使って検出する

OpenCV
│
├─ カメラ画像を取得
├─ 四角や点を描く
├─ 文字を描く
└─ ウィンドウ表示

Pythonのロジック
│
├─ ROI内判定
├─ WORKING状態管理
├─ 時間計測
└─ 累積時間計算
```

つまりYOLOが直接、

> この人は機械で作業中です

と判断しているわけではありません。

YOLOが判断しているのは基本的に、

> **ここにpersonがいます**

という部分です。

そこから、

> ROI内にpersonがいる\
> → 作業中とみなす

という業務上の意味付けをしているのは、
今回自分たちで作ったPythonのロジックです。

この区別は非常に重要です。

------------------------------------------------------------------------

# 41. AI部分と普通のプログラム部分を分けて考える

今回のプログラムは全部がAIではありません。

``` text
カメラ画像
    ↓
[ AI ]
YOLO人物検出
    ↓
Bounding Box
    ↓
[ 通常のプログラム ]
中心点計算
    ↓
ROI判定
    ↓
状態管理
    ↓
時間計測
```

AIはシステムの一部分です。

工場システムでは、

> AIに何でも判断させる

よりも、

> AIには得意な「人物検出」を担当させ、
> その後の判定は明確なルールで処理する

という構成が分かりやすく、調整もしやすいです。

------------------------------------------------------------------------

# 42. 今回まず覚えたいYOLO用語

  用語               今回の意味
  ------------------ ------------------------------------------
  YOLO               物体検出モデル
  model              読み込んだYOLOモデル
  inference / 推論   学習済みモデルで新しい画像を判定すること
  class              物体の種類
  person             今回検出したいクラス
  confidence         検出の信頼度
  Bounding Box       検出物体を囲む長方形
  `xyxy`             左上と右下によるBounding Box座標
  frame              カメラ動画を構成する1枚の画像
  ROI                判定対象とする領域
  predict            学習済みモデルを使って推論する処理

------------------------------------------------------------------------

# 43. この段階での理解目標

最初からYOLOのニューラルネットワーク内部をすべて理解する
必要はありません。

まずは、

1.  カメラ動画はフレームの連続である
2.  1フレームずつYOLOへ渡している
3.  YOLOが人物のBounding Boxを返す
4.  `classes=[0]` でpersonだけに限定している
5.  `conf` で信頼度のしきい値を指定する
6.  Bounding Boxから人物位置を計算する
7.  ROI内かどうかはPython側で判定する
8.  作業時間の計測もPython側のロジックである

この8点が理解できれば、今回のプログラムの骨格は十分に 理解できています。

------------------------------------------------------------------------

# 44. 今後の学習ロードマップ

このプロジェクトを使ってYOLOを勉強し直すなら、 次の順番がおすすめです。

``` text
STEP 1
現在の人物検出プログラムを理解
    ↓
STEP 2
Bounding Box / confidence / classを理解
    ↓
STEP 3
ROI判定を理解
    ↓
STEP 4
中心点 → 足元判定へ変更
    ↓
STEP 5
3秒程度の検出切れ対策
    ↓
STEP 6
作業開始・終了をSQLiteへ保存
    ↓
STEP 7
複数作業エリアへの対応
    ↓
STEP 8
必要になった場合だけ人物追跡を検討
    ↓
STEP 9
必要になった場合だけ独自学習を検討
```

「YOLOそのものの勉強」と「工場アプリの実用化」を
同時に少しずつ進められるロードマップです。

------------------------------------------------------------------------

# 45. まとめ

今回のサンプルで最も重要なのは、

``` text
YOLOで人を検出する
```

だけではなく、

``` text
YOLO
  ↓
人物の位置を取得
  ↓
ROIとの位置関係を判定
  ↓
工場にとって意味のある「作業中」という情報へ変換
```

という流れです。

YOLOは「personがどこにいるか」を教えてくれます。

しかし、

> その人が機械の周りで作業しているか

という工場固有の判断は、ROIや時間条件などを使って
アプリケーション側で定義します。

この考え方を押さえておけば、今後、

-   作業者検知
-   安全エリア侵入検知
-   滞在時間計測
-   人数カウント
-   設備との連携

などへ発展させる際にも応用できます。

今回の `detect_worker.py` は、そのための非常に良い 出発地点です。
